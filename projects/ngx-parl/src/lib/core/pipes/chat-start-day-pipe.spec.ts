import { ChatStartDayPipe } from './chat-start-day-pipe';

describe('ChatStartDayPipe', () => {
  it('create an instance', () => {
    const pipe = new ChatStartDayPipe();
    expect(pipe).toBeTruthy();
  });
});
